<view class="content">
  <view class="img-item" wx:for="{{imgList}}" wx:key="*this">
    <image src="{{item}}" mode="widthFix"></image>
  </view>
  <view class="btn text-c">
    <navigator url="{{nearShopUrl}}">
      优惠券可用门店
    </navigator>
  </view>
</view>